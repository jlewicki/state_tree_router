name: state_tree_router
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  async: ^2.8.2
  collection: ^1.16.0
  logging: ^1.0.2
  # tree_state_machine: ^2.3.0
  tree_state_machine: 
    path: ../tree_state_machine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
